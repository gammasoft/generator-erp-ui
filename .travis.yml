language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-erp-ui' ]; then cd .. && eval "mv $currentfolder generator-erp-ui" && cd generator-erp-ui; fi

